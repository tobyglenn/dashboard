# nt-001: Verify AsyncStorage Persistence in nutritionStore

**Status:** Review  
**Assignee:** Clarity  
**Epic:** NutriTrack BJJ Buddy

## Current State

`nutritionStore.ts` already has `persist` middleware with `AsyncStorage` implemented (lines 3-4, 82-183). The infrastructure is in place.

## Task

Verify it actually works end-to-end — persistence bugs are often in the `partialize` config or storage key conflicts.

## Steps

1. Read `nutrition-app/nutritrack/src/store/nutritionStore.ts` lines 80-190
2. Confirm `partialize` is correctly including `entries`, `goals`, and `dailyTotals`
3. Run the app: `cd nutrition-app/nutritrack && npx expo start --web --port 8083`
4. Add a food entry, hard-refresh browser, confirm entry persists
5. If it doesn't persist, check for storage key conflicts or missing fields in `partialize`

## Key Files
- `nutrition-app/nutritrack/src/store/nutritionStore.ts` — persist config at lines 82-185

## Success Criteria
- Food entries survive page refresh
- Goals survive page refresh
- No TypeScript errors
